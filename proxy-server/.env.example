# Proxy Server Configuration

# Ollama settings
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5-coder:latest

# MCP bridge settings
# Point this at a local HTTP bridge that exposes /health, /connections, /connect, /tools/list, and /tools/call.
MCP_BRIDGE_URL=http://localhost:8790

# Proxy server
PORT=8787
